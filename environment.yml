name: bigquery-mcp-oauth
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    - google-auth-oauthlib>=1.2.0
    - google-auth>=2.23.0
    - mcp>=1.0.0
    - httpx>=0.25.0
